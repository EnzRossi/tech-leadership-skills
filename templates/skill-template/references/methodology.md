# Methodology

Detailed method for the skill. This file is loaded only when SKILL.md says to read it, so it can be longer and more thorough than SKILL.md.

Recommended sections:

- The dimensions the analysis covers, with what "strong", "weak", and "unknown" look like for each
- How the dimensions combine into a recommendation (decision rules, not a formula that hides judgment)
- Worked example: a short input and the resulting output
- Failure patterns the method is designed to catch

Label EnzRossi's own methodology as such and attribute borrowed concepts to their sources, which are listed in `sources.md`.
